/**
 * This is something to think about!
 *
 */

package test

import foo.bar.* 
import baz.FooBar

/** bla */

/* asdasd */

@Package 
class Alice : Bob, Eve
{

	@Private
	var variable : Foobar = store *= 5 += true and (1 + 2) - table.data[1,2,3] == 3 > 3 not in 7 or 42

	@Protected
	@beagle.compiler.Optimize
	const constant = "My constant"

	@Public	
	def myMethod( argument : double ) : int
	{
		return constant * argument
	}

}